import 'package:flutter/material.dart';

class InstagramDownloaderScreen extends StatelessWidget {
  const InstagramDownloaderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Instagram Downloader'),
      ),
    );
  }
}
